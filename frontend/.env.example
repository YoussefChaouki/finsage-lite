# FinSage-Lite Frontend — Environment Variables
# Copy to .env.local for local development (git-ignored)

# Base URL of the FastAPI backend
# Local dev (outside Docker): http://localhost:8000
# Docker Compose (inside network): http://api:8000
VITE_API_URL=http://localhost:8000
